# Optional: the local website creates .env when it asks for a key. Never commit .env.
OPENAI_API_KEY=
EVOCOUP_OPENAI_MODEL=gpt-5.6-terra
EVOCOUP_REASONING_EFFORT=low
EVOCOUP_OPENAI_TIMEOUT_SECONDS=90
